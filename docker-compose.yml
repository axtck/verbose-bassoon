version: "3.9"

services:
  sample:
    container_name: verbose-bassoon-rr
    build:
      context: .
      dockerfile: Dockerfile  # change to Dockerfile.prod when in prod
    volumes:
      - ".:/app"
      - "/app/node_modules"
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
